{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./development && rm -rf ./.cache && parcel src/index.html --out-dir development -p 5000",
    "build": "parcel build src/index.html --out-dir dist --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "author": "Makdoom Shaikh",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  },
  "dependencies": {
    "gh-pages": "^3.1.0",
    "jquery": "^3.5.1",
    "lodash.debounce": "^4.0.8",
    "lodash.throttle": "^4.1.1",
    "scroll-out": "^2.2.11",
    "typed.js": "^2.0.11"
  }
}
